[project]
name = "streamforge"
version = "0.1.0"
description = "Real-time event streaming and analytics platform"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]